/**
 * Seed script for Iliadis Executive Cars.
 *
 * Populate the database with initial vehicles or admin users by extending this file.
 * Example usage:
 *   const prisma = new PrismaClient();
 *   await prisma.user.create({ data: { email: 'admin@example.com', password: hash } });
 */
export {};
